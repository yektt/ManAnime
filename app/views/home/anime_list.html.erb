<%= render partial: 'display_5content', locals: {category: 'Most popular ' + @popular.length.to_s + ' ' + pluralize_contents(@popular), content: @popular} %>
<%= render partial: 'display_5content', locals: {category: 'Most recent ' + @recent.length.to_s + ' ' + pluralize_contents(@recent), content: @recent} %>
<%= render partial: 'display_5content', locals: {category: 'Most popular & not completed ' + @unfinished_popular.length.to_s + ' ' + pluralize_contents(@unfinished_popular), content: @unfinished_popular} %>

<%= render partial: 'display_all', locals: { episode_or_chapter: 'Episodes', content: @all} %>
